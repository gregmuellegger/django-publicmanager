[buildout]
parts = python django
eggs = 
unzip = true

[python]
recipe = zc.recipe.egg
interpreter = python
eggs = ${buildout:eggs}

[django]
recipe = djangorecipe
version = 1.2.1
project = django_publicmanager
projectegg = django_publicmanager
settings = testsettings
test = django_publicmanager
eggs = ${buildout:eggs}
extra-paths =
  src
